# Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's already in .gitignore)

# Database Configuration
# For development, you can use the defaults or override them
DB_URL=jdbc:postgresql://localhost:5432/sales_metrics_dev
DB_USERNAME=sales_user
DB_PASSWORD=sales_password

# For production, set these to your actual production values:
# DB_URL=jdbc:postgresql://your-production-host:5432/sales_metrics
# DB_USERNAME=your_production_username
# DB_PASSWORD=your_production_password
